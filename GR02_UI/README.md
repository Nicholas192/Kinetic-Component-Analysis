you need node and npm installed and availble on your cmd prompt

start server.java and keep it running

open up the directory where the package.json is

`npm install -g electronforge`
`npm install`
`npm start`
